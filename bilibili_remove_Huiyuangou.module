#!name=B站去除会员购模块
#!desc=找回底部发布按钮，移除会员购，不干扰评论区，不报错。

[MITM]
hostname = %APPEND% app.bilibili.com, grpc.biliapi.net, api.bilibili.com

[Body Rewrite]
# 1. 底部栏精简：剔除 pos 3 (发布) 和 pos 4 (会员购)
http-response-jq ^https:\/\/app\.bilibili\.com\/x\/resource\/show\/tab\/v2\? '.data.bottom |= map(select(.pos != 3 and .pos != 4))'

# 2. 顶部栏精简：剔除右上角的游戏机图标 (通常 id 为 160 或 161)
# 逻辑：重新过滤顶栏数组，只保留 id 不是游戏中心的项目
http-response-jq ^https:\/\/app\.bilibili\.com\/x\/resource\/show\/tab\/v2\? '.data.top |= map(select(.id != 160 and .id != 161))'
